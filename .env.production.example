# =============================================================
# eGramSabha Production Environment Configuration
# =============================================================
# INSTRUCTIONS:
#   1. Copy this file to .env.production on the production server:
#        cp .env.production.example .env.production
#   2. Replace <YOUR_SERVER_IP> with your EC2 Elastic IP address
#      (or your domain name if you have one)
#   3. Fill in API keys where indicated
#   4. The deploy script reads .env.production and generates .env
# =============================================================

# ---- Frontend Settings ----
FRONTEND_PORT=80
FRONTEND_API_URL=http://<YOUR_SERVER_IP>/api
FRONTEND_BACKEND_URL=http://<YOUR_SERVER_IP>
APP_INFRA=JIO

# ---- Backend Settings ----
BACKEND_PORT=5000
BACKEND_URL=http://<YOUR_SERVER_IP>
CORS_ORIGIN=http://<YOUR_SERVER_IP>
STORAGE_BACKEND=gridfs
VIDEO_MOM_BACKEND_URL=http://video-mom-backend:8000
NODE_ENV=production

# ---- JWT Secrets (CHANGE THESE — use random strings in production) ----
# Generate with: openssl rand -hex 32
JWT_ADMIN_SECRET=CHANGE_ME_admin_secret_replace_with_random_string
JWT_OFFICIAL_SECRET=CHANGE_ME_official_secret_replace_with_random_string
JWT_CITIZEN_SECRET=CHANGE_ME_citizen_secret_replace_with_random_string
JWT_REFRESH_SECRET=CHANGE_ME_refresh_secret_replace_with_random_string

# ---- MongoDB Settings ----
MONGODB_URI=mongodb://mongodb:27017/voter_registration
MONGODB_URL=mongodb://mongodb:27017
MONGODB_PORT=27017
DATABASE_NAME=voter_registration

# ---- JioMeet Settings ----
# Get your App ID from https://jiomeetpro.jio.com
JIOMEET_API=https://jiomeetpro.jio.com/api/platform/v1
JIOMEET_APP_ID=your_jiomeet_app_id_here
PRIVATE_KEY_PATH=./keys/RSA-PrivateKey.pem
PUBLIC_KEY_PATH=./keys/RSA-PublicKey.pub

# ---- HuggingFace / AI Settings ----
# Get your token at https://huggingface.co/settings/tokens
HF_TOKEN=your_huggingface_token_here
STT_MODEL_ENDPOINT=https://router.huggingface.co/hf-inference/models/openai/whisper-large-v3-turbo
HUGGING_FACE_LLM_ENDPOINT=https://router.huggingface.co/cohere/compatibility/v1/chat/completions
HF_LLM=command-a-03-2025

# Jio Translate API key — obtain from your Jio AI platform account
JIO_API_KEY=your_jio_api_key_here

STT_PROVIDER=whisper
LLM_PROVIDER=huggingface
TRANSLATION_PROVIDER=llm

# ---- Support Settings ----
SUPPORT_EMAIL=support@egramsabha.gov.in
SUPPORT_PHONE=1800-XXX-XXXX
SUPPORT_CONTACT_NAME=eGramSabha Support Team

# ---- Cron Settings (production: every 5 min instead of every 1 min) ----
AGENDA_TRANSLATION_CRON=*/5 * * * *
INITIATE_SUMMARY_CRON=*/5 * * * *
FETCH_SUMMARY_RESULTS_CRON=*/5 * * * *
RETRY_FAILED_SUMMARY_CRON=*/10 * * * *
